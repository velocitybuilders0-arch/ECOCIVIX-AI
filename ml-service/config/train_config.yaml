base_model: "distilbert-base-uncased"
num_labels: 4
max_len: 128
epochs: 3
learning_rate: 2.0e-5
batch_size: 16
weight_decay: 0.01
warmup_ratio: 0.1
seed: 42
label_order: ["LOW", "MEDIUM", "HIGH", "CRITICAL"]
